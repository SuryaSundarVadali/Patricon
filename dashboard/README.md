# Patricon Dashboard

This package provides an operational view of Patricon agents, policies, proof submissions, and transaction outcomes.

## Scripts

- `pnpm dev`: Start local dashboard development server.
- `pnpm build`: Build production assets.
- `pnpm test`: Run dashboard tests.

## Scope

The dashboard is intentionally lightweight for early deployment phases and will evolve with policy and settlement telemetry.
